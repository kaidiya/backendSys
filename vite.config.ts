import {UserConfig} from 'vite';

/**
 * @type {import('vite').UserConfig}
 */
 const config:UserConfig = {
  // ...
  root: './',
  mode: 'development',
  base: './',
  build: {
    
  },
}

export default config